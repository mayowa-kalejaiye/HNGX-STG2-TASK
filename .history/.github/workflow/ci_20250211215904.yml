name: CI Pipeline

on:
  pull_request:
    branches:
      - main # Runs on PRs to the main branch

jobs:
  tests:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Set up Python
